'use strict';

/**
 * advisory-team-title service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::advisory-team-title.advisory-team-title');
