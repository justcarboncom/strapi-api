'use strict';

/**
 *  advisory-team-title controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::advisory-team-title.advisory-team-title');
