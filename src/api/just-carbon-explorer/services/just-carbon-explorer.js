'use strict';

/**
 * just-carbon-explorer service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::just-carbon-explorer.just-carbon-explorer');
