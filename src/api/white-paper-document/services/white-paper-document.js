'use strict';

/**
 * white-paper-document service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::white-paper-document.white-paper-document');
